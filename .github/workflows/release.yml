name: Release Extension

on:
  push:
    tags:
      - '[0-9][0-9][0-9][0-9].[0-9][0-9].[0-9][0-9]*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: syntagmax-vscode/package-lock.json

      - name: Install dependencies
        run: |
          cd syntagmax-vscode
          npm install

      - name: Compile and Package
        run: |
          cd syntagmax-vscode
          npx @vscode/vsce package --out syntagmax.vsix

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: syntagmax-vscode/syntagmax.vsix
          generate_release_notes: true
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
